export enum DiceTarget {
    Attack = 'attack',
    Defense = 'defense',
}

export enum BonusTarget {
    Hp = 'hp',
    Speed = 'speed',
}

export enum DieKind {
    D4 = 'd4',
    D6 = 'd6',
    None = 'none',
}

export enum StatKey {
    Hp = 'hp',
    Speed = 'speed',
    Attack = 'attack',
    Defense = 'defense',
}